apiVersion: v1
kind: Service

metadata:
  name: product-service

spec:
  selector:
    app: product

  ports:
    - port: {{ .Values.product.port }}
      targetPort: {{ .Values.product.port }}

  type: ClusterIP